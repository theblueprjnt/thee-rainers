---
// Permanent 301 redirect — /workshop and /defense-workshop were two separate
// pages for the same live event with independently drifting copy and pricing
// logic. /defense-workshop is the correct, unconditionally-live version.
export const prerender = false;
return Astro.redirect('/defense-workshop', 301);
---
